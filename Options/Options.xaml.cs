namespace NinaTheSkyX.Options {

    /// <summary>
    /// Code-behind de Options.xaml. Toutes les interactions sont câblées
    /// via Command bindings dans le XAML — pas de handlers click nécessaires.
    /// </summary>
    public partial class Options : System.Windows.ResourceDictionary {
        public Options() {
            InitializeComponent();
        }
    }
}
